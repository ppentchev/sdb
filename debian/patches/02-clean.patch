Description: Fix the "clean" target.
Forwarded: not yet
Author: Peter Pentchev <roam@ringlet.net>
Last-Update: 2013-03-27

--- a/src/Makefile
+++ b/src/Makefile
@@ -59,4 +59,4 @@
 	${CC} ${CFLAGS} -o ${BIN} main.o ${OBJ}
 
 clean:
-	rm -rf ${OBJ} libsdb.a a.out sdb sdb.dSYM *.sdb *.db *.${SOEXT}
+	rm -rf ${OBJ} ${SOBJ} main.o libsdb.a a.out sdb sdb.dSYM *.sdb *.db *.${SOEXT}
--- a/bindings/vala/Makefile
+++ b/bindings/vala/Makefile
@@ -21,7 +21,7 @@
 	@${CC} -o mctest mctest.c ${MCSDB_FLAGS} ${GFLAGS}
 	
 clean:
-	rm -f test a.out test.sdb test.c uuid.sdb
+	rm -f test a.out mctest.c mctest test.sdb test.c uuid.sdb
 	cd types && ${MAKE} clean
 
 .PHONY: all clean
